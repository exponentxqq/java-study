package com.study.base.encryption;

/**
 * 公钥加密，私钥解密，就是非对称加密
 *
 * <p>私钥加密，公钥解密，就是签名
 *
 * <p>签名的意义在于：确认某个信息确实是由某个发送方发送的，任何人都不可能伪造消息，并且发送方也不能抵赖
 *
 * <p>在实际应用的时候，签名实际上并不是针对原始消息，而是针对原始消息的哈希进行签名
 */
public class Sign {
  public static void main(String[] args) {}
}
